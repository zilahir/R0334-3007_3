import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'R0334-3007_3',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
